# AWS Next Terraform module

Terraform module which creates the resources to host a nextjs web app on AWS.

## Usage

```hcl
module "next" {
  source      = "genstackio/layer-next/aws"

  // ...
}
```
